export default function StrategiesPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-gray-100 mb-1">Strategies</h1>
      <p className="text-sm text-gray-500">
        Browse all strategy versions, compare metrics across versions, and inspect
        the Strategy IR (JSON). All versions are stored permanently — nothing is deleted.
        Implemented in Phase 2.
      </p>
    </div>
  );
}
