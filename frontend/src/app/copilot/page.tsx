export default function CopilotPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-gray-100 mb-1">AI Co-Pilot</h1>
      <p className="text-sm text-gray-500">
        Conversational strategy design interface. Describe your strategy in plain English
        and the AI proposes a Strategy IR. Full RAG context from all prior conversations,
        backtests, and trade outcomes. Split-view: chat on left, Strategy IR inspector on right.
        Implemented in Phase 2.
      </p>
    </div>
  );
}
